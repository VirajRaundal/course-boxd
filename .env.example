# Root environment variables shared across workspaces
# Copy this file to .env and adjust values as needed.

# General
NODE_ENV=development

# Web application defaults
NEXT_PUBLIC_APP_NAME=CourseBoxd
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Authentication
AUTH_SECRET=your-auth-secret
EMAIL_FROM="CourseBoxd <no-reply@courseboxd.app>"
EMAIL_SERVER=smtp://user:password@smtp.example.com:587

# Database (PostgreSQL)
# For Supabase: Replace <project-ref> with your Supabase project reference and <db-password>
# with the database password generated in the Supabase dashboard.
# DATABASE_URL uses pgBouncer for connection pooling (for application runtime)
# DIRECT_DATABASE_URL bypasses pgBouncer (required for migrations and seeding)
#
# For local PostgreSQL setup:
# 1. Install PostgreSQL (https://www.postgresql.org/download/)
# 2. Create a database: createdb courseboxd_dev
# 3. Use the following URLs:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/courseboxd_dev
# DIRECT_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/courseboxd_dev
#
# Supabase connection strings:
DATABASE_URL=postgresql://postgres:<db-password>@db.<project-ref>.supabase.co:5432/postgres?pgbouncer=true&connection_limit=1&sslmode=require
DIRECT_DATABASE_URL=postgresql://postgres:<db-password>@db.<project-ref>.supabase.co:5432/postgres?sslmode=require
